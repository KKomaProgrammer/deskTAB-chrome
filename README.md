# deskTAB Chrome prebuilt runtime

Generated automatically. Contains the prebuilt ARM64 Ubuntu + XFCE + Google Chrome runtime used by the Android fast installer.
The archive uses zstd so extraction on ARM Android is substantially faster than the previous xz runtime.
